---
'@fleetia/lagrange': minor
---

Add a compact ColorPicker with an anchored dialog, keyboard-operable hue wheel, saturation and lightness controls, editable color channels, and native color selection that preserves alpha. Values use the existing ColorField normalized hex contract.

Keep controlled HSL inputs synchronized when a parent rejects an edit, and preserve precise alpha values when an unchanged channel loses focus.
